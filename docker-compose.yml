services:
  db:
    image: postgres
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - ./schema:/docker-entrypoint-initdb.d:z
    ports:
      - "5432:5432"
  
  back:
    build: 
      context: .
      dockerfile: Containerfile
    ports:
      - "8000:8000"
  


